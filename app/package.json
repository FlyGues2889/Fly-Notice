{
  "name": "fly-notice",
  "version": "0.0.1",
  "description": "A notice displayer",
  "main": "main.js",
  "icon": "app/assets/appIcon.ico",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "packager": "electron-packager . FlyNotice --win --out ./out --icon=assets/appIcon.ico --overwrite"
  },
  "author": "FlyGues",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
  }
}
